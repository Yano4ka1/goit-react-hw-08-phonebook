import styled from '@emotion/styled';
import {
  typography,
  color,
  space,
  layout,
  shadow,
  border,
  flexbox,
  grid,
} from 'styled-system';

export const Box = styled('div')(
    typography,
    color,
    space,
    layout,
    shadow,
    border,
    flexbox,
    grid
);